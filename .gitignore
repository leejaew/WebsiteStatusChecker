# Python bytecode and caches
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/
# Legacy bundled venv from the old Replit Python template
venv (deactivated)/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Test / coverage artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local environment files (never commit secrets)
.env
.env.local
.env.*.local

# Replit-managed runtime/agent state.
# The repo is intentionally Replit-agnostic — `.replit` stays on disk
# locally (the deployment needs it) but is not version-controlled.
.replit
.local/
.agents/
.cache/
.upm/
.pythonlibs/
.breakpoints

# Legacy template documentation (no longer applicable)
VENV.md
